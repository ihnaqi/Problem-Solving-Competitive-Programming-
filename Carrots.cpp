#include <bits/stdc++.h>
using namespace std;

int main()
{

   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   int n;
   int p;
   cin >> n >> p;
   while (n--)
   {
      string str;
      cin >> str;
   }
   cout << p << "\n";

   return 0;
}