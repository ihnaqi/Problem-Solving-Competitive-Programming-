#include <bits/stdc++.h>
using namespace std;

int main()
{
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   cout.tie(NULL);
   string name;
   getline(cin, name);
   cout << "Thank you, " << name << ", and farewell!\n";
   return 0;
}